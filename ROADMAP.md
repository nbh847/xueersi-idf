# 项目路线图

本文件记录项目当前状态。硬件事实与协议查阅 `README.md`，代码结构与开发入口查阅 `docs/project-overview.md`，目标架构查阅 `docs/xiaomiao_firmware_v0.1_design.md`。每个 Goal 的施工文档保留在 `goals/`，用于沉淀范围、决策、验收证据和限制；完成后不删除，当前状态以本文件为准。

## 当前状态

- ESP32 固件基于 ESP-IDF 6.1 与 LVGL 9.5，普通固件开机默认进入 `main/framework/` 的 Launcher；Launcher 按注册顺序显示 `Games`（`main/apps/games/` 占位 App）、`PC Monitor`（`main/apps/pc_monitor/` 静态骨架 App）、`Tools`（`main/apps/tools/` 菜单 App）、`Settings`（`main/apps/settings/` 设置骨架 App）与 `Hardware Test`（`main/main.c` 中的 15 页 Hardware Dashboard）五个入口：前四项占满第 1 页 2 列 × 2 行网格，`Hardware Test` 位于第 2 页。
- Dashboard 已覆盖 15 个页面：光照、热敏、运动、LED1、LED2、蜂鸣器、电机 1、电机 2、MicroSD、GPIO25、GPIO26、ADC32、ADC33、系统和 About。
- ESP32 端已接入 ST7735、六键输入、ADC、LEDC、SPI MicroSD、GD32 `0x40` 与 MPU6050 `0x68`。
- GD32 工程当前只确认实现 USB CDC、USART1 桥和 ESP32 IO0/EN 控制；I2C `0x40` LED/电机从机协议未在仓库源码中实现。
- Service 分层已建立：节点 9 新增 `main/services/` 并实现首个 System Service `Settings Service`（默认值、校验、NVS 读写与安全回退，App 不直接访问 NVS），已完成实机验证；BSP 分层仍处于设计阶段。App Framework 核心运行时（App 描述、Registry、Manager）已于节点 1 实现，Navigation 统一进入／返回与内容根对象所有权已于节点 2 实现，Launcher 两列首页、焦点与分页已于节点 3 实现，均位于 `main/framework/`；节点 4 已把 15 页 Dashboard 注册为 `Hardware Test` App 并把普通固件默认入口切换为 Launcher，可执行的启动、生命周期、输入和 15 页人工测试均已通过；节点 5 新增首个独立业务目录 `main/apps/games/`（`Games` 占位 App），普通固件按 Games → Hardware Test 顺序注册两个入口，双入口顺序、双 App 焦点保持、多轮生命周期和 Hardware Test 回归均已实机验证通过；节点 6 新增第二个业务目录 `main/apps/pc_monitor/`（`PC Monitor` 静态骨架 App，四项指标恒显 `--`），普通固件改为按 Games → PC Monitor → Hardware Test 注册三个入口，三入口顺序、短按 B 返回、`screen children` 稳定与既有 App 回归均已实机验证通过；节点 7 新增第三个业务目录 `main/apps/tools/`（`Tools` 菜单 App，Wi-Fi／System Info／About 三项 + 两级 B + 真实只读系统信息），普通固件改为按 Games → PC Monitor → Tools → Hardware Test 注册四个入口，四个入口顺序、`screen children` 恒定、Hardware Test 索引 3、三套 B 语义并存与既有 App 回归均已实机验证通过。节点 8 已新增第四个业务目录 `main/apps/settings/`（`Settings` 菜单 App），普通固件改为按 Games → PC Monitor → Tools → Settings → Hardware Test 注册五个入口，第 1 页 2 列 × 2 行满页、Hardware Test 首次落到第 2 页；Settings 在节点 8 只提供 `Wi-Fi`／`Display`／`Sound`／`System` 四项 UI 骨架与只读能力状态页（其中 `Display` 与 `System` 已在节点 9 改为陈述固定背光事实与真实持久化状态），Wi-Fi、显示控制与 Audio Service 仍未实现；其可执行人工回归已于 2026-09-20 完成（五入口注册、12 次 open／close 严格配对、`screen children` 恒为 2、Hardware Test 焦点索引 4 与页码 1、Settings 10 次往返；首轮 Monitor 日志未覆盖 Games 与 PC Monitor 的进入／返回，2026-09-20 14:08 已由人工补充确认，见 `goals/20260920-1338-settings-ui.md`）。

## 当前开发节点

- 节点 0“构建基线恢复”已完成（2026-09-19，全新配置构建、烧录和基础交互验证通过；未覆盖全部外设回归）。施工文档为 `goals/20260919-1834-build-baseline.md`。
- 节点 1“App Framework”已完成（2026-09-20，框架代码、自测代码、普通构建、自测构建、自测固件 PASS 标记和普通固件 Dashboard 回归均通过）。施工文档为 `goals/20260919-2037-app-runtime.md`。
- 节点 2“Navigation”已完成（2026-09-20，导航运行时 + 自测 + 自测固件实机 PASS + 普通构建 15 页 Dashboard 回归全部通过）。施工文档为 `goals/20260920-0816-navigation.md`。
- 节点 3“Launcher”已完成（2026-09-20，运行时代码 + 自测代码 + 自测构建实机 `LAUNCHER_SELF_TEST: PASS` + 普通构建 15 页 Dashboard 回归与视觉三项确认全部通过）。本节点先通过专用自测构建验收动态入口、焦点和分页，普通固件继续启动 Dashboard，节点 4 接入 Hardware Test App 后再切换默认启动。施工文档为 `goals/20260920-1007-launcher.md`。
- 节点 4“Hardware Test App”已完成（2026-09-20）。普通固件默认进入 Launcher，15 页 Dashboard 注册为 `Hardware Test` App，B 手势判定通道固定为 `keypad_read_cb()` 边沿加独立状态机（对象级事件回调通道经核对不可行）。实机验证：检查点 1（11:20）；生命周期 16 轮／连续 11 轮且 `screen children` 恒为 2、B 短按与长按、15 页可达、MicroSD 缺失路径（11:43）。唯一未验证条款为“从电机或蜂鸣器运行状态退出时先停止持续输出”，因本机无电机硬件、GD32 无 `0x40` 从机实现、蜂鸣器仅鸣叫 140 ms 而无法构造，按本 Goal 失败路径条款标记未验证且不阻塞；**LED、电机、MPU6050 的实机行为仍无证据，不得视为外设回归已通过**。施工文档为 `goals/20260920-1053-hardware-test-app.md`。
- 节点 5“Games 占位 App”已完成（2026-09-20）。新增 `main/apps/games/xiaomiao_games.{h,c}`（首个独立业务目录）、`main/CMakeLists.txt` 源文件与 `INCLUDE_DIRS "."`、`main/main.c` 的 `launcher_register_app()` 与 Games → Hardware Test 注册顺序。实机验证：`launcher created (2 apps)`；Games 10 轮与 Hardware Test 4 轮 open/close 严格配对，`screen children=2` 在全部 28 条日志上恒定，无 panic／重启；Games 短按 B 返回（后 8 轮均 <1 s），Hardware Test 4 次返回全部 `launcher focus=1 page=0`（节点 4 单 App 阶段无法证明的焦点保持首次取得数值证据）；两种 B 语义互不干扰，15 页无回归。**两处数量口径差异**：条款要求“一次不中断连续 10 轮”与“至少 5 轮双 App 交替”，实测为 10 轮 Games（最长连续段 4 轮）与 4 轮 Hardware Test，经人工确认按现有证据接受，原文记录于 Goal。施工文档为 `goals/20260920-1159-games-placeholder.md`。
- 节点 6“PC Monitor UI”已完成（2026-09-20）。新增第二个业务目录 `main/apps/pc_monitor/xiaomiao_pc_monitor.{h,c}`（`icon = LV_SYMBOL_EYE_OPEN`、固定列宽两列指标行、四项值恒为编译期占位 `-- %`／`-- %`／`-- %`／`-- °C`、无数据源）、`main/CMakeLists.txt` 的 `APP_SRCS` 扩展与 `main/main.c` 中间位注册，普通固件改为 Games → PC Monitor → Hardware Test 三入口。实机验证（12:52）：`launcher created (3 apps)` 与 `Launcher ready, 3 app(s) registered`；8 轮进入／返回全部严格配对，**`screen children=2` 在全部 16 条 open／close 日志上恒定**，无 panic／重启；Hardware Test 2 次返回均为 `launcher focus=2 page=0`，两种 B 语义并存互不干扰。**轮次口径差异**：条款要求“连续 10 轮 PC Monitor”与“至少 5 轮三 App 交替”，实测为 PC Monitor 4 轮、Games 2 轮、Hardware Test 2 轮共 8 轮，经人工确认按现有证据接受，原文记录于 Goal。**焦点索引 0／1 无日志数值证据**，依赖人工目视与日志顺序一致性（同节点 5 口径）。设计文档第 15 节的节点 11 通信与动态数据仍属后续。施工文档为 `goals/20260920-1235-pc-monitor-ui.md`。
- 节点 7“Tools App”已完成（2026-09-20 13:28）。新增第三个业务目录 `main/apps/tools/`（`Tools` 菜单 App：Wi-Fi／System Info／About 三项 + 两级 B + 真实只读系统信息），普通固件改为按 Games → PC Monitor → Tools → Hardware Test 注册四个入口，第 1 页 2 列 × 2 行恰好占满。实机验证：`launcher created (4 apps)` 与 `Launcher ready, 4 app(s) registered`；6 次 open 对 6 次 close 严格配对，**`screen children=2` 在全部 12 条 open／close 日志上恒定**；`hardware test closed` 输出 `launcher focus=3 page=0`，与 Hardware Test 索引由 2 变为 3 一致（关键数值证据）；Games 491 ms、PC Monitor 815 ms 短按返回，Hardware Test 3614 ms 长按返回，Tools 3 次会话各只 close 一次且耗时 6194／21932／13502 ms，三套 B 语义并存互不干扰；无 panic／重启／输入失效。**轮次口径差异**：条款要求 Tools 累计 ≥10 次往返，实测 Tools 3 次、四 App 共 6 次（四 App 交替 ≥5 轮满足），经人工确认按现有证据接受。**证据类型差异**：Tools 自身的 Launcher 焦点索引无串口数值证据（只有 Hardware Test 打印 `focus=`），索引 2 为“Games 关闭后下一次进入即 Tools，0→2 只能由 DOWN 到达”的间接证据；System Info 等视图内容为人工目视确认（视图切换为 `ESP_LOGD`，默认级别不输出）。施工文档为 `goals/20260920-1258-tools-app.md`。
- 节点 8“Settings UI”已完成（2026-09-20 14:03）。新增第四个业务目录 `main/apps/settings/`（`Settings` 菜单 App：`Wi-Fi`／`Display`／`Sound`／`System` 四项 + 两级 B + 只读能力状态页），普通固件改为按 Games → PC Monitor → Tools → Settings → Hardware Test 注册五个入口，第 1 页 2 列 × 2 行满页、Hardware Test 首次落到第 2 页。实机验证：`launcher created (5 apps)` 与 `Launcher ready, 5 app(s) registered`；12 次 open 对 12 次 close 严格配对（Hardware Test 1、Tools 1、Settings 10），**`screen children=2` 在全部 24 条 open／close 日志上恒定**；`hardware test closed` 输出 **`launcher focus=4 page=1`**，同时以数值证据证明索引由节点 7 的 3 后移到 4 且落到第 2 页；Settings 累计 10 次往返 ≥ 条款要求的 5，其中 6 s 内连续 5 轮满足“连续 ≥5 轮”；无 panic／重启／输入失效。**口径差异**：条款要求“五个 App 各完成至少一次进入／返回”，实测 Games 与 PC Monitor 本轮未进入，经人工确认按现有证据接受（两者源码未改动且已在节点 5／6 独立验证）；2026-09-20 14:08 人工已补充确认这两项的进入／返回通过并给出“节点 8 功能验收无待办”的结论，未提供补充串口日志。**证据类型差异**：Settings 自身不打印焦点索引（决策 9 要求不依赖 Launcher），索引 3、页码 1/2、四项菜单与四个状态页内容、Hardware Test 15 页均为目视确认（视图切换为 `ESP_LOGD`，默认级别不输出）。施工文档为 `goals/20260920-1338-settings-ui.md`。
- 节点 9“Settings Service 与 NVS”已完成（2026-09-20）。新增 `main/services/xiaomiao_settings_service.{h,c}`（首个 System Service，`main/services/` 目录由此建立）与 `main/services/xiaomiao_settings_service_selftest.{h,c}`（6 步自测入口），`main/CMakeLists.txt` 增加 `SERVICE_SRCS`、`nvs_flash` 依赖与 `XIAOMIAO_SETTINGS_SERVICE_SELF_TEST` 选项，`main/main.c` 在硬件与 LVGL 初始化前调用 `xiaomiao_settings_service_init()`（失败只记警告）并新增第四个互斥自测形态，`main/apps/settings/xiaomiao_settings.c` 的 `Display` 页改为陈述固定背光事实、`System` 页改为读取真实持久化来源与错误摘要。首版配置只有 `wifi_auto_connect`／`sound_enabled`（默认均 `true`），NVS 固定写在默认 `nvs` 分区的 `xiaomiao/settings`（schema v1、8 字节固定宽度 blob、公开结构不按内存布局序列化），缺失 key 写默认值、blob 非法只重写本 key、读取失败降级为内存默认值，源码中无 `nvs_flash_erase()`、无自定义分区表。实机验证：普通构建与烧录通过（`App version: 7088147-dirty`），首启 `source=defaults`、断电重启后 `source=nvs`，两次均到达 `Launcher ready, 5 app(s) registered`；6 步自测固件输出 `SETTINGS_SERVICE_SELF_TEST: PASS`；Settings 四页文案与五 App 回归由人工确认“都没问题”（末轮未提供补充串口日志）。**唯一未验证项**：`nvs_set_blob()`／`nvs_commit()` 提交失败与 `nvs_flash_init()`／`nvs_open()` 直接失败两条路径无法在不引入自定义分区表的前提下构造，只按源码检查确认，不阻塞收口。施工文档为 `goals/20260920-1429-settings-service-nvs.md`。
- GD32 实机固件与 README 中 `0x40` 协议的对应关系仍在待确认状态，不阻塞不依赖 LED、电机的 v0.1 框架开发。

## 有序开发节点

节点定义、交付和验收标准见 `docs/xiaomiao_firmware_v0.1_design.md` 第 15 节；此处只维护唯一进度状态。

- [x] 节点 0：构建基线恢复。
- [x] 节点 1：App Framework。
- [x] 节点 2：Navigation。
- [x] 节点 3：Launcher。
- [x] 节点 4：Hardware Test App。
- [x] 节点 5：Games 占位 App。
- [x] 节点 6：PC Monitor UI。
- [x] 节点 7：Tools App。
- [x] 节点 8：Settings UI。
- [x] 节点 9：Settings Service 与 NVS。
- [ ] 节点 10：Wi-Fi Service。
- [ ] 节点 11：PC Monitor 通信。
- [ ] 节点 12：Audio Service。
- [ ] 节点 13：首个正式游戏。
- [ ] 节点 14：Storage Service。
- [ ] 节点 15：Assets 与文件系统。
- [ ] 节点 16：GD32 `0x40` 协议补全。

## 下一步

1. 节点 9“Settings Service 与 NVS”已完成（2026-09-20，普通构建、首启／重启持久化、6 步自测固件与 Settings UI／五 App 回归均已验证），施工文档为 `goals/20260920-1429-settings-service-nvs.md`。**节点 10“Wi-Fi Service”尚未立项**，是本节点的下一个开发节点；Settings 的 `Wi-Fi` 页与 Tools 的 Wi-Fi 详情页在节点 10 前仍显示 Service 不可用。可选补录：末轮 Settings 四页与五 App 回归为人工口头确认，未提供补充串口日志；如需数值证据，固件已在板上，重新 `idf.py -p COM5 monitor` 后各按一轮即可，无需重新编译或烧录。
2. GD32 `0x40` 协议补全（节点 16）完成前，LED、电机、MPU6050 的实机回归无法闭环；节点 4 已按设备缺失处理并记录，不重复阻塞后续节点。
3. 可选补录（不影响节点 4 结论）：缺失设备页面的实际显示文本（GD32 `0x40` 的 LED／电机页、MPU6050 的运动页），以及挂载失败后重新进入 App 的行为。
4. 可选补录（不影响节点 5 结论）：条款“一次不中断连续 10 轮 Games 进入／返回”与“至少 5 轮双 App 交替”未取得满足口径的样本（实测 10 轮 Games 最长连续段 4 轮、Hardware Test 4 轮），经人工确认按现有证据接受；如需补齐，固件已在板上，重新 `idf.py -p COM5 monitor` 后补按若干轮即可，无需重新编译或烧录。
5. 按需另立任务定位 SD／GPIO22 配置冲突（`docs/xiaomiao_firmware_v0.1_design.md` 第 9 节已预告，不属节点 4）。
6. 已实现的节点 4／5 约束（供后续复核，非待办）：B 的短按／长按判定基于 `keypad_read_cb()` 的按下与释放边沿加独立状态机，动作在 LVGL 循环执行；`ui_key_event_cb()` 中原有的 `LV_KEY_ESC` 立即分支已移除，B 只有一个决策点；`lv_indev_set_long_press_time()` 全局设置未改（方向键按住重复依赖它）。Launcher 在“App 打开”状态下转发 B 的分支只在 App 未取得输入焦点时可达：Hardware Test 会把输入对象加入 group，该分支对它不可达（属预期，节点 4 已记录）；Games 不加入 group，该分支对它可达，并已在节点 5 实机验证（短按 B 即返回）。未修改 `main/framework/` 下节点 1～3 文件。

## 待确认与已知风险

- `docs/xiaomiao_firmware_v0.1_design.md` 第 9 节记录的 `sdmmc_card_init failed` 与 `gpio: conflict found for GPIO[22]` 已于 2026-09-20 实机复现两次（11:32 按 A 三次失败三次、11:43 按 A 两次失败两次，`PIN_NUM_SD_CS = GPIO_NUM_22`）。两次均未阻塞启动、进入与返回，符合设计文档“失败则 SD_UNAVAILABLE、记录日志、继续启动 Launcher”的既定策略；但配置 SD 卡时的具体冲突机制尚未定位（第二次及后续尝试在挂载前即报 GPIO22 冲突，提示 CS 引脚未被上一次失败释放），需另立任务，不属节点 4 范围。
- README 记录的 GD32 LED/电机协议已被 ESP32 代码使用，但当前 GD32 工程缺少对应实现，双 MCU 联调结果待确认。
- 本机 ESP-IDF 安装为非默认布局：Python venv 3.14.7 位于 `<IDF_TOOLS_PATH>/tools/python/v6.1/venv`（不在 `<IDF_TOOLS_PATH>/python_env/` 下），需进程级设置 `IDF_TOOLS_PATH` 与 `IDF_PYTHON_ENV_PATH` 后再运行官方 `export.ps1`；constraints 文件曾错位于工具目录的 `tools/` 子目录，2026-09-19 经授权复制到工具目录根修复（原文件保留）。MSYS/Git Bash 中执行 export 会因 `MSYSTEM` 变量被拒绝。PATH 中的 `idf.py.exe`（idf-exe 1.0.3 包装器）`--version` 显示包装器自身版本，确认 IDF 版本需用 `python "$env:IDF_PATH\tools\idf.py" --version`。
- 节点 0～9 的可执行人工验证均已完成；节点 8 首轮日志未覆盖 Games 与 PC Monitor，2026-09-20 14:08 已由人工补充确认全部手动测试通过，但未提供这两项的补充串口日志。**节点 9 的末轮 Settings 四页与五 App 回归同为人工口头确认，无补充串口日志**；其两条无法构造的失败路径（提交失败、`nvs_flash_init()`／`nvs_open()` 直接失败）只按源码检查确认，已在 Goal 中记录。LED、电机、MPU6050 的真实设备行为仍需相应硬件与 GD32 `0x40` 协议配合验证，MicroSD 缺失路径已覆盖。PC Monitor 的真实指标数据与通信属节点 11，尚未开始。Tools 的视图内容为目视确认（视图切换为 `ESP_LOGD`，默认日志级别不输出），Tools 自身的 Launcher 焦点索引无串口数值证据。

## 最近完成

- 2026-09-20 20:16：完成节点 9“Settings Service 与 NVS”，标记为已完成。人工完成普通固件构建、烧录与 Monitor（首启 `source=defaults`、断电重启后 `source=nvs`，两次均 `Launcher ready, 5 app(s) registered`）、隔离自测构建与 6 步自测固件（`SETTINGS_SERVICE_SELF_TEST: PASS`），并在烧回普通固件后确认 Settings 四项详情页与五 App 回归“都没问题”；20:16 用户再次明确确认节点 9 开发与手动测试全部通过。末轮为人工确认，无补充串口日志，已在“最近验证”中如实记录；两条无法构造的失败路径只按源码检查确认。同步更新 `goals/20260920-1429-settings-service-nvs.md`、`README.md`、`docs/project-overview.md`、`AGENTS.md` 与 `ROADMAP.md` 的目录职责与状态描述。
- 2026-09-20：实现节点 9“Settings Service 与 NVS”源码，节点标记为**已实现、待人工验证**。新增 `main/services/xiaomiao_settings_service.{h,c}`（首个 System Service，`main/services/` 由此建立）与 `main/services/xiaomiao_settings_service_selftest.{h,c}`（6 步自测入口）；公开结构只含 `wifi_auto_connect`／`sound_enabled`（默认均 `true`），持久化写在默认 `nvs` 分区的 `xiaomiao/settings`（schema v1、8 字节固定宽度 blob、`_Static_assert` 固定布局、公开结构不按内存布局序列化、保留位清零且不承载语义）；恢复顺序为 key 缺失→写默认值、读取失败→降级、blob 非法→只重写本 key、有效→装载；`set()` 通过 `nvs_commit()` 后才替换内存快照，失败保留旧快照并返回原始错误；`init()` 幂等（重复调用返回首次结果、不重写 Flash）；`nvs_flash_init()`／`nvs_open()` 失败只记原始错误码并降级，不使用 `ESP_ERROR_CHECK`、不擦分区、不阻塞启动。`main/CMakeLists.txt` 新增 `SERVICE_SRCS`、`nvs_flash` 依赖与 `XIAOMIAO_SETTINGS_SERVICE_SELF_TEST` 选项；`main/main.c` 在硬件与 LVGL 初始化前调用 `xiaomiao_settings_service_init()`（失败只记一条警告）并新增第四个互斥自测形态；`main/apps/settings/xiaomiao_settings.c` 把统一的 `settings_build_status()` 改为通用 `settings_build_detail()`，`Display` 页陈述固定背光事实，新增 `settings_build_system()` 每次进入读取一次真实来源与错误摘要（`Loaded from NVS`／`Defaults applied`／`Defaults restored`／`Not persisted`／`NVS error 0x…`），不创建 timer、不提供控件。自测用 `esp_restart()` 在 6 步之间自动重启并以 `xiaomiao/selftest_stage` 键推进，覆盖缺失 key、有效 v1 重启恢复、未知版本、越界布尔值、短于 v1 与长于 v1 的 blob，末步恢复干净状态并输出 `SETTINGS_SERVICE_SELF_TEST: PASS`；全程只用 `nvs_erase_key()` 处理自有的两个 key，不调用 `nvs_flash_erase()`。未修改 `main/framework/`、Games、PC Monitor、Tools、Dashboard 15 页与 B 状态机、GD32、硬件协议、引脚、ESP-IDF／LVGL 版本或 `dependencies.lock`。同步更新 `goals/20260920-1429-settings-service-nvs.md`、`README.md`、`docs/project-overview.md` 与 `AGENTS.md`。**编译、烧录与全部实机验收尚未执行。**
- 2026-09-20 14:29：创建节点 9“Settings Service 与 NVS”施工文档并标记为已立项、待实现。固定 `main/services/xiaomiao_settings_service.{h,c}` 边界、`wifi_auto_connect=true` 与 `sound_enabled=true` 两个首版字段、默认 `nvs` 分区的 `xiaomiao/settings` schema v1、固定宽度 blob、缺失／非法／不可用回退和提交成功后才更新内存快照的语义；明确禁止整分区擦除、自定义分区、Wi-Fi／Audio／显示控制、敏感数据和后台任务。Settings UI 只增加真实只读状态：Display 陈述固定背光，System 展示配置来源；Wi-Fi／Sound 在后续 Service 实现前不提供开关。尚未修改固件源码或执行构建、烧录与实机验证。
- 2026-09-20 14:03：完成节点 8“Settings UI”，标记为已完成。人工完成 ESP-IDF 6.1 普通构建、烧录与 Monitor，提供自启动 778 ms 至运行 389095 ms 的完整串口日志，并确认五入口布局、四项菜单与四个状态页、跨页与空槽、两级 B、焦点与页码保持、既有 App 回归“没问题”。日志复核：启动链 `Xiaomiao LVGL 9.5 launcher boot` → `Start Xiaomiao launcher` → `launcher created (5 apps)` → `Launcher ready, 5 app(s) registered`，全程无 self-test 标记、无旧 dashboard 启动行、banner 仅一次；12 次 `<app> opened` 对 12 次 `<app> closed` 严格配对（Hardware Test 1、Tools 1、Settings 10），无孤立 open、无重复 close；**`screen children=2` 在全部 24 条 open／close 日志上恒定**；`hardware test closed` 输出 **`launcher focus=4 page=1`**，索引 4 与注册顺序（Games 0、PC Monitor 1、Tools 2、Settings 3、Hardware Test 4）一致且 `page=1` 即第 2 页，首次以数值证据同时证明第 1 页被 4 个入口占满、Hardware Test 落到第 2 页；Settings 累计 10 次往返 ≥ 条款要求的 5，383020→389095 的 6 s 内连续 5 轮满足“连续 ≥5 轮”，10 次会话各只 close 一次且含 24340 ms 长会话与 373／436 ms 短会话，符合“单次物理按压只产生一次层级变化”；时间戳单调递增，无 panic／看门狗／重启循环／输入失效。**口径差异**：条款要求“五个 App 各完成至少一次进入／返回”，本轮日志只有 Hardware Test、Tools 与 Settings，**Games 与 PC Monitor 未进入**；经提出后人工明确选择按现有证据接受，理由为两者源码在本节点未改动（`git status` 已确认）且已在节点 5／6 独立实机验证，补齐无需重新编译或烧录。**证据类型差异**：页码 2/2 与索引 4 为数值证据；第 1 页四格布局与页码 1/2、第 2 页空槽行为、Settings 四项菜单与四个状态页内容、Settings 索引 3 与返回后停留第 1 页、二级 B 的实际层级变化、Hardware Test 15 页（该次会话仅 2236 ms，属快速检查）均为人工目视确认，Settings 按决策 9 不打印 Launcher 焦点索引。本轮未进入 MicroSD 页，未复现 SD／GPIO22 冲突。同步更新 `goals/20260920-1338-settings-ui.md`、`README.md`、`docs/project-overview.md`、`docs/xiaomiao_firmware_v0.1_design.md` 与 `AGENTS.md` 的目录职责描述。
- 2026-09-20 13:45：实现节点 8“Settings UI”源码。新增 `main/apps/settings/xiaomiao_settings.{h,c}`：头文件只暴露 `const xiaomiao_app_t *xiaomiao_settings_app(void)`；`.c` 内定义固件全生命周期有效的静态描述（`id=settings`、`name=Settings`、`icon=LV_SYMBOL_EDIT`、`init=NULL`），私有视图枚举 `SETTINGS_VIEW_{MENU,WIFI,DISPLAY,SOUND,SYSTEM}`，菜单为 `Wi-Fi`／`Display`／`Sound`／`System`（上下键 0～3 边界停止、A 进入详情、左右无操作），四个只读状态页统一为“能力名 + `Unavailable` + 实现节点”（Wi-Fi → node 10、Display／System → node 9、Sound → node 12），无控件、无虚假状态、无持久化提示；输入焦点由 Settings 根对象经 `lv_group_get_default()` 取得；B 用“按下锁存 + 20 ms LVGL timer 释放检测”隔离 LVGL 长按重复事件，菜单页关闭动作在该 timer 内执行以避免在焦点对象自己的按键回调里删除它；`close` 顺序为删 timer → 移出 group → 清空引用，内容根仍由 Navigation 在回调返回后删除；四行菜单几何为标题 y=2 高 16、菜单 y=20 起行高 20 步进 22（末行 86～106）、页脚 y=110，适配 160 × 128。`main/CMakeLists.txt` 的 `APP_SRCS` 扩展为 games／pc_monitor／tools／settings 四项（不新增组件或依赖）；`main/main.c` 在 Tools 与 Hardware Test 之间插入 `launcher_register_app(xiaomiao_settings_app())` 并补齐 `launcher_boot()` 注释的节点来源。未修改 `main/framework/`、Games、PC Monitor、Tools、Dashboard 15 页与 B 状态机、GD32、硬件协议、引脚、依赖版本或 `dependencies.lock`。编译、烧录与实机验证尚未执行。
- 2026-09-20 13:28：完成节点 7“Tools App”，标记为已完成。人工完成 ESP-IDF 6.1 普通构建、烧录与 Monitor，提供自启动 750 ms 至运行 62211 ms 的完整串口日志，并确认四入口布局、Tools 三项菜单与三个详情页、System Info 显示值、Wi-Fi 与 About 页面、两级 B 与长按隔离、既有 App 回归“都测试过了”。日志复核：`launcher created (4 apps)`／`Launcher ready, 4 app(s) registered`，全程无 self-test 标记与旧 dashboard 启动行；6 次 open 对 6 次 close 严格配对（Games 1、PC Monitor 1、Hardware Test 1、Tools 3），无孤立 open、无重复 close；**`screen children=2` 在全部 12 条 open／close 日志上恒定**；`hardware test closed` 输出 `launcher focus=3 page=0`，与 Hardware Test 索引由 2 变为 3 一致；日志中的切换序列（HT(3) → UP → PC Monitor(1) → LEFT → Games(0) → DOWN → Tools(2)）完全符合 2 列 × 2 行规则，反向印证 Tools 位于索引 2；Games 491 ms、PC Monitor 815 ms 短按返回，Hardware Test 3614 ms 长按返回，Tools 3 次会话各只 close 一次且耗时 6194／21932／13502 ms（说明会话内有多级往返且长按未穿透两层），三套 B 语义并存互不干扰；时间戳单调递增，无 panic／看门狗／重启循环／输入失效。**轮次口径差异**：条款要求 Tools 累计 ≥10 次往返，实测 Tools 3 次、四 App 共 6 次（四 App 交替 ≥5 轮满足），经人工确认按现有证据接受。**证据类型差异**：索引 3 为数值证据，索引 2 为“Games 关闭后下一次进入即 Tools，0→2 只能由 DOWN 到达”的间接证据，索引 0／1 为目视与顺序一致性证据；System Info 等视图内容为人工目视确认（视图切换用 `ESP_LOGD`，默认级别不输出）。本轮未进入 MicroSD 页，未复现 SD／GPIO22 冲突。同步更新 `goals/20260920-1258-tools-app.md`、`README.md`、`docs/project-overview.md` 与 `AGENTS.md` 的目录职责描述。
- 2026-09-20 13:16：实现节点 7“Tools App”源码。新增 `main/apps/tools/xiaomiao_tools.{h,c}`：头文件只暴露 `const xiaomiao_app_t *xiaomiao_tools_app(void)`；`.c` 内定义固件全生命周期有效的静态描述（`id=tools`、`name=Tools`、`icon=LV_SYMBOL_LIST`、`init=NULL`），菜单为 `Wi-Fi`／`System Info`／`About`（上下键 0～2 边界停止、A 进入详情、左右无操作），`System Info` 每次进入用只读 API 采集芯片族与核心数／`CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ`／`esp_flash_get_size()`／`heap_caps_get_total_size(MALLOC_CAP_SPIRAM)`／`esp_get_idf_version()`／`esp_app_get_description()->version`，Flash 读失败显示 `Unknown` 并记错误码、PSRAM 为 0 显示 `None`，全部 `snprintf` 固定缓冲；`Wi-Fi` 页固定显示 `Wi-Fi Service`／`Unavailable`／`Implemented in node 10`，未调用任何 `esp_wifi_*`；`About` 显示项目、实际固件版本、作者与仓库标识。输入焦点由 Tools 根对象经 `lv_group_get_default()` 取得；B 用“按下锁存 + 20 ms LVGL timer 释放检测”隔离 LVGL 长按重复事件，菜单页关闭动作在该 timer 内执行以避免在焦点对象自己的按键回调里删除它。`main/CMakeLists.txt` 的 `APP_SRCS` 扩展为 games／pc_monitor／tools 三项并新增 `esp_app_format` 依赖（`main.c` 现有 include 路径不含 `esp_app_desc.h`，已从 `build/compile_commands.json` 核实）；`main/main.c` 在 PC Monitor 与 Hardware Test 之间插入 `launcher_register_app(xiaomiao_tools_app())`。未修改 `main/framework/`、Games、PC Monitor、Dashboard 15 页与 B 状态机、GD32、硬件协议、引脚、LVGL／IDF 版本或 `dependencies.lock`。编译、烧录与实机验证尚未执行。
- 2026-09-20 13:10：按人工确认修订 `goals/20260920-1258-tools-app.md` 的轮次口径条款。节点 5／6 的“连续 10 轮”“至少 5 轮交替”两次都因实测样本不足走人工豁免，为避免节点 7 重演，把检查点 4、检查点 5 与验收标准第 12 条改为“累计 ≥N 次往返 + open／close 成对日志计数”的可满足表述，并在该文档新增修订记录说明理由与判定实质不变。仅改文档，未改源码。
- 2026-09-20 12:58：创建节点 7“Tools App”施工文档并标记为已立项、待实现。明确独立 `main/apps/tools/` 模块、Games → PC Monitor → Tools → Hardware Test 注册顺序、Wi-Fi／System Info／About 三项菜单、真实只读系统信息来源、Wi-Fi Service 未实现状态、LVGL 默认 group 输入、两级 B 返回及 ESC 长按重复隔离、四入口满页导航、多轮生命周期和既有 App 回归标准；明确排除节点 10 的 Wi-Fi 初始化／扫描／连接／重连、Settings／NVS 与后台任务。尚未修改固件源码或执行人工构建与实机验证。
- 2026-09-20 12:52：完成节点 6“PC Monitor UI”，标记为已完成。人工完成 ESP-IDF 6.1 普通构建、烧录与 Monitor 并提供 `app_main` 起至运行 99.2 s 的完整串口日志，确认三入口视觉与按键路径“都没问题”。日志复核：`launcher created (3 apps)`／`Launcher ready, 3 app(s) registered`；8 轮进入／返回全部严格配对，**`screen children=2` 在全部 16 条 open／close 日志上恒定**；Hardware Test 2 次返回均为 `launcher focus=2 page=0`（第三入口索引）；Games 1256／1851 ms 与 PC Monitor 1648／1903／8482／10352 ms 均为短按即返，Hardware Test 3124／7745 ms 均超 800 ms 阈值，两种 B 语义互不干扰；时间戳单调递增，无 panic／重启／输入失效。**轮次口径差异**：条款要求“连续 10 轮 PC Monitor”与“至少 5 轮三 App 交替”，实测 PC Monitor 4 轮、Games 2 轮、Hardware Test 2 轮共 8 轮，经提出后人工确认按现有证据接受（条款实质已在 8 次往返、16 条日志上覆盖），原文与推理保留在 Goal 的“验证结果”。**焦点索引 0／1 无日志数值证据**（只有 Hardware Test 的 close 路径打印 `launcher focus=`），依赖人工目视与日志顺序一致性，与节点 5 口径一致。同步更新 `goals/20260920-1235-pc-monitor-ui.md`、`ROADMAP.md`、`README.md`、`docs/project-overview.md` 与 `AGENTS.md` 的目录职责描述。
- 2026-09-20 12:42：实现节点 6“PC Monitor UI”源码。新增 `main/apps/pc_monitor/xiaomiao_pc_monitor.{h,c}`：头文件只暴露 `const xiaomiao_app_t *xiaomiao_pc_monitor_app(void)`；`xiaomiao_pc_monitor.c` 定义固件全生命周期有效的静态描述（`id=pc_monitor`、`name=PC Monitor`、`icon=LV_SYMBOL_EYE_OPEN`、`init=NULL`），`open` 只在 `xiaomiao_navigation_app_root()` 下创建全屏容器、标题 `PC Monitor`、CPU／RAM／GPU／TEMP 四行（名称列 x=8 宽 60 左对齐、值列 x=92 宽 60 右对齐，独立标签加固定列宽，不使用空格对齐）与页脚 `B Back`，四项值均为编译期静态占位 `-- %`／`-- %`／`-- %`／`-- °C`（`°` 以显式 UTF-8 字节写出以保持源码纯 ASCII）；`close` 只清空容器引用交给 Navigation 删除内容根。`main/CMakeLists.txt` 的 `APP_SRCS` 扩展为 games 与 pc_monitor 两项；`main/main.c` 在 Games 与 Hardware Test 之间插入 `launcher_register_app(xiaomiao_pc_monitor_app())`。未修改 `main/framework/`、Games、Dashboard 15 页与 B 状态机、GD32、硬件协议、引脚、依赖版本或 `dependencies.lock`；未引入数据源、Service、Task、timer 或通信代码。编译、烧录与实机验证尚未执行。
- 2026-09-20 12:35：创建节点 6“PC Monitor UI”施工文档并标记为已立项、待实现。明确独立 `main/apps/pc_monitor/` 模块、静态 App 描述、Games → PC Monitor → Hardware Test 注册顺序、CPU／RAM／GPU／TEMP 四项 `--` UI、Launcher 短按 B 返回、三入口焦点与奇数项网格导航、多轮生命周期和既有 App 回归标准；明确排除 PC 数据采集、通信协议、Wi-Fi、后台任务、动态数据与更新接口。尚未修改固件源码或执行人工构建与实机验证。
- 2026-09-20 12:29：完成节点 5“Games 占位 App”，标记为已完成。人工完成 ESP-IDF 6.1 普通构建、烧录与 Monitor 并确认视觉与按键路径正常；日志复核通过启动注册 2 个 App、Games 10 轮与 Hardware Test 4 轮 open/close 严格配对、`screen children=2` 在全部 28 条日志上恒定、Hardware Test 4 次返回全部 `launcher focus=1 page=0`（节点 4 无法证明的焦点保持首次取得数值证据）、Games 后 8 轮返回耗时均 <1 s（短按 B 生效）与 Hardware Test 每轮 >1.9 s（长按生效）并存不干扰。两处数量口径差异（缺“一次不中断连续 10 轮”与“第 5 轮双 App 交替”，实测为最长 4 轮连续段与 4 轮 HT）经人工确认按现有证据接受，原文与推理保留在 Goal 的“验证结果”。同步更新 `ROADMAP.md`、`docs/project-overview.md`；`AGENTS.md` 的目录职责描述已随 `main/apps/` 的建立做最小事实订正。
- 2026-09-20 12:15：实现节点 5“Games 占位 App”代码。新增首个独立业务目录 `main/apps/games/`：`xiaomiao_games.h` 只暴露 `const xiaomiao_app_t *xiaomiao_games_app(void)`，`xiaomiao_games.c` 内定义固件全生命周期有效的 `static const` 描述（`id=games`、`name=Games`、`icon=LV_SYMBOL_PLAY`、`init=NULL`），`open` 只在 `xiaomiao_navigation_app_root()` 下创建占位容器与 `Games`／`Coming soon`／`B Back` 三个标签，`close` 只清空容器引用交给 Navigation 删除内容根。`main/CMakeLists.txt` 新增 `APP_SRCS` 并把 `INCLUDE_DIRS ""` 改为 `"."`（原值为空，导致 `main/` 自身不在 include 路径上，`apps/games/` 无法按 `framework/...` 引用框架头文件）；`main/main.c` 新增 `launcher_register_app()` 并改为按 Games → Hardware Test 顺序注册两个 App。未修改 `main/framework/`、Dashboard 15 页、GD32、硬件协议、引脚、依赖版本或 `dependencies.lock`。编译、烧录与实机验证尚未执行。
- 2026-09-20 11:59：修正路线图中“普通固件仍以单文件 Dashboard 为入口”的过期表述，并创建节点 5“Games 占位 App”施工文档；明确独立业务目录、静态描述接口、Games → Hardware Test 注册顺序、占位 UI、Launcher B 返回分支、双 App 焦点保持、多轮生命周期和 Hardware Test 回归标准。尚未修改固件源码或执行人工构建与实机验证。
- 2026-09-20 11:46：完成节点 4“Hardware Test App”，标记为已完成。人工确认蜂鸣器实际发声正常、本机无电机硬件，据此查明验收条款“从电机或蜂鸣器运行状态退出时先停止持续输出”在当前硬件上无法构造：电机侧 `s_board.motor_running[]` 只在 `gd32_motor_set()` 返回 `ESP_OK` 时置真，无电机且 GD32 无 `0x40` 从机实现故从未为真，`hardware_test_close()` 的停止循环被 `continue` 跳过；蜂鸣器侧 A 键仅鸣叫 140 ms（`buzzer_beep(s_buzzer_freq_hz, 140)`，`main/main.c:2065`）后自动停止，不存在“仍在发声”时按 B 的时刻，单键模型下也无法同时按住 A 与 B。该条款按本 Goal 失败路径条款标记未验证且不阻塞；同时明确 LED、电机、MPU6050 的实机行为仍无证据，不得视为外设回归已通过。同步更新 `ROADMAP.md` 与 Goal。
- 2026-09-20 11:32：为节点 4 检查点 2 补充可观测日志。原实现没有任何输出可用于核对“screen 子对象数量保持基线”，该验收条款实际不可测量，因此在 `hardware_test_open()` 末尾输出活动 screen 的子对象数量、在 `hardware_test_close()` 末尾输出同一数值以及 `xiaomiao_launcher_focused_index()`／`xiaomiao_launcher_page_index()`。前者用于发现内容根泄漏（应恒为 Launcher 根 + 新 App 内容根），后者作为“返回后与进入前一致”的证据。仅新增日志，未改变任何控制流与功能。
- 2026-09-20 11:18：实现节点 4“Hardware Test App”代码。`main/main.c` 新增 `Hardware Test` App 描述（`id=hardware_test`、`name=Hardware Test`、`icon=LV_SYMBOL_SETTINGS`，`init` 为空，硬件仍在 `app_main` 初始化一次）与 `hardware_test_open/close/is_open`；`ui_create()` 改为接收 Navigation 内容根作为父对象，不再使用 `lv_screen_active()`；`close` 先取消 B 手势、停蜂鸣器与两路电机（失败记录错误码但不阻塞），再从 group 移除输入对象并清空全部 `s_ui` 引用。B 键改为 `keypad_read_cb()` 边沿驱动的独立状态机（800 ms 长按、300 ms 后复用 `set_action()` 显示 `Hold to exit` 提示），动作在 LVGL 循环执行；`ui_key_event_cb()` 中立即执行 `ui_cancel()` 的 `LV_KEY_ESC` 分支已移除。`lvgl_task()` 改为注册 App、`init_all()`、创建 Launcher，任一步失败记录阶段与错误码且不回退到 Dashboard；`hardware_update()`／`ui_refresh()` 仅在 Hardware Test 打开时执行，`hardware_process_timers()` 保持常驻。启动日志改为 `Xiaomiao LVGL 9.5 launcher boot` 与 `Start Xiaomiao launcher`。未修改 `main/framework/`、GD32、硬件协议、引脚、依赖版本或 `dependencies.lock`。
- 2026-09-20 11:10：修订节点 4 施工文档，落实三点既定结论：保留 800 ms 长按方案并在决策 8 记录否决“按页面状态自适应”语义的理由（`ui_cancel()` 在电机页未运行时的分支是切换方向，`main/main.c:2029-2046`）；决策 9 新增长按可发现性提示（按住达 300 ms 复用 `set_action()` 显示一次，不新增 UI 元素、不改 15 页布局）；补充短按延迟量级说明（一个按键时长，几十到一百多毫秒，受 800 ms 阈值约束）。
- 2026-09-20 11:02：补齐节点 4 施工文档的 B 键判定通道决策。原决策 9 允许在“对象级事件回调”与“小型状态机”之间二选一，经查本仓库锁定的 LVGL 9.5 `indev_keypad_proc()` 后确认事件回调通道不可行，改为强制使用 `keypad_read_cb()` 的按下／释放边沿加独立状态机：阈值 800 ms 用 `lv_tick_get()` 计算，状态机在回调内只记录手势、`ui_cancel()` 与 `xiaomiao_navigation_back()` 在 LVGL 循环执行，不得改全局按键时间。同时明确删除 `ui_key_event_cb()` 原有 `LV_KEY_ESC` 立即分支使 B 只有一个决策点，禁止改动 `main/framework/` 下节点 1～3 文件，并修正施工文档中 `main/main.c` 的行数表述。仅修改文档，未改动固件源码。

## 最近验证

- 2026-09-20 20:16（节点 9 收尾复核，用户确认、Agent 静态检查）-- 通过：用户明确确认节点 9 开发完成且手动测试全部通过；Agent 复核 Service、6 步自测、启动链、Settings UI、CMake 和项目文档，未发现阻塞缺陷。`git diff --check` 通过，仅有工作区既有 LF→CRLF 提示；改动范围与 Goal 一致，未出现 `nvs_flash_erase()` 调用、自定义分区、Wi-Fi／Audio／硬件控制或后台任务。人工构建、烧录、自测与实机结果沿用本节此前记录，本次按项目分工未重复运行 `idf.py` 或占用串口。
- 2026-09-20（节点 9 Settings UI 与五 App 回归，人工执行、Agent 记录）-- 通过：人工将普通固件烧回目标板后完成实机验证，确认 Settings 四项详情页文案与布局、Wi-Fi／Sound 页不出现开关、五 App 进入／返回与既有行为“都没问题”。**本次只有人工口头确认，未提供补充串口日志**，因此不补造 `open`／`closed` 时间戳、`screen children` 数值或焦点索引，生命周期与持久化类证据仍以 19:34 与自测条目的串口日志为准。节点 9 的可执行人工验证自此完整覆盖，功能验收无待办；唯一未验证项是两条无法构造的失败路径（`nvs_set_blob()`／`nvs_commit()` 提交失败、`nvs_flash_init()`／`nvs_open()` 直接失败），只按源码检查确认且不阻塞。
- 2026-09-20（节点 9 自测固件 6 步，人工执行、Agent 复核）-- 通过：人工用 `-D XIAOMIAO_SETTINGS_SERVICE_SELF_TEST=ON` 在 `.tmp/build-settings-selftest/` 隔离构建（编译 exit=0，10 条 `-Wunused-function` 警告属预期：自测分支不初始化 LCD／LVGL，与 Framework 自测构建同源），烧录后提供末步日志：`settings_selftest: Settings service self test, step 6/6` → `settings_svc: reading settings failed: ESP_ERR_NVS_INVALID_LENGTH (0x110c), using memory defaults` → `settings_svc: settings service degraded, source=degraded` → `SETTINGS_SERVICE_SELF_TEST: PASS`。**通过的有效性依据**：自测的 `xiaomiao/selftest_stage` 标记只在当前整步的全部断言通过后才推进（失败会打印 FAIL 并挂起，不会重启也不推进），因此能进入第 6 步本身就证明第 1～5 步全部通过，即已覆盖：缺失 key 装载并持久化默认值、非默认值写入后跨真实重启恢复、未知 schema 版本回退、越界布尔值回退、短于 v1 的 blob 回退，以及每步附带断言的“只重写 `xiaomiao/settings`、namespace 内其他 key 未被触碰”。第 6 步另断言长 blob 被 `nvs_get_blob()` 以 `ESP_ERR_NVS_INVALID_LENGTH` 拒绝、来源置为 `degraded`、快照仍为可读默认值、已存条目的 16 字节未被覆盖，以及失败后 `init()` 仍幂等。**仍未覆盖**：`nvs_set_blob()`／`nvs_commit()` 提交失败与 `nvs_flash_init()`／`nvs_open()` 直接失败的路径（不引入自定义分区表无法构造，只能源码检查）；Settings 四项详情页文案与布局目视；五 App 与 Hardware Test 回归、`screen children` 基线。
- 2026-09-20 19:34（节点 9 普通构建首启与重启持久化，人工执行、Agent 复核）-- 部分通过：人工完成 ESP-IDF 6.1 普通构建（`App version: 7088147-dirty`、`Compile time: Sep 20 2026 19:34:22`）与烧录 Monitor，提供两次完整启动日志。**首次启动**输出 `settings_svc: settings service ready, source=defaults (wifi_auto_connect=1, sound_enabled=1)`，即 key 缺失时装载默认值并写入成功（恢复规则 1）；**人为断电重启后**（`rst:0x1 (POWERON_RESET)`，属真实冷启动而非软复位）输出 `settings_svc: settings service ready, source=nvs (wifi_auto_connect=1, sound_enabled=1)`，即配置从 NVS 成功加载；两次启动均继续走完启动链并输出 `launcher created (5 apps)` 与 `Launcher ready, 5 app(s) registered`，证明 Settings Service 接入未破坏启动链、且 NVS 写入后重新打开可读回。启动日志无 self-test 标记、无旧 dashboard 启动行、无 panic／看门狗／重启循环、无 `nvs_flash_erase` 相关行为。分区表输出确认默认 `nvs` 分区存在（`nvs WiFi data 01 02 0000a000 00006000`），未引入自定义分区表。**本次仍未覆盖**：Settings 四项详情页文案与布局、Wi-Fi／Sound 页无开关的边界、五 App 与 Hardware Test 回归、`screen children` 基线，以及 6 步自测固件（非默认值重启恢复的“值”级证据、未知版本／越界布尔值／短 blob 回退、长 blob 降级仍全部未取得）。节点 9 仍不得标记为已完成。
- 2026-09-20（节点 9 实现后静态检查）-- 仅静态检查，运行时全部未验证：`git diff --check` 通过（仅既有 LF→CRLF 提示）；新增文件无行尾空白、无 Tab、纯 ASCII（首次写入的 5 处中文引文已改为英文后复检通过）。大括号配平：`xiaomiao_settings_service.c` 36/36、`.h` 3/3、`xiaomiao_settings_service_selftest.c` 34/34、`.h` 1/1、`xiaomiao_settings.c` 63/63、`main/main.c` 354/354、`main/CMakeLists.txt` 8/8。改动范围核对：新增 `main/services/` 四个文件，修改 `main/CMakeLists.txt`（`SERVICE_SRCS`、`nvs_flash`、第四个自测选项）、`main/main.c`（新增 include、启动链最早处调用 `xiaomiao_settings_service_init()`、新增第四个互斥自测形态）、`main/apps/settings/xiaomiao_settings.{h,c}`，未修改 `main/framework/`、`main/apps/games/`、`main/apps/pc_monitor/`、`main/apps/tools/`、`GD32_firmware/`、`sdkconfig.defaults`、`sdkconfig.ci`、`main/idf_component.yml`、`dependencies.lock`、引脚或硬件协议。禁止项检索：`main/services/` 内无 `esp_wifi`／`ledc_`／`gpio_`／`i2c_`／`spi_`／`sdmmc`／`sdspi`／`xTaskCreate`／`xQueue`／`xSemaphore`／`xEventGroup`／`esp_timer_`／`lv_`；全仓库内 `nvs_flash_erase` 仅出现在自测文件的一句注释中（说明未调用）；`main/apps/settings/xiaomiao_settings.c` 与 `main/main.c` 内无 `nvs_`／`nvs_flash` 调用；未新增自定义分区表或 `partitions.csv`。ESP-IDF 6.1 行为核对：`nvs_get_blob()` 在存储条目长于缓冲区时返回 `ESP_ERR_NVS_INVALID_LENGTH` 并回写实际长度、短于缓冲区时返回 `ESP_OK` 并回写真实长度（`components/nvs_flash/src/nvs_api.cpp:588-616`）；`Storage::writeItem()` 覆盖同 key 旧条目（含长度或旧类型不同，`nvs_storage.cpp:392-551`）；`Page::findItem()` 同 key 不同类型返回 `ESP_ERR_NVS_TYPE_MISMATCH`（`nvs_page.cpp:1128-1134`）；默认 C 标准为 `gnu23`（`tools/cmake/build.cmake:207`），`_Static_assert` 可用。**未执行**：ESP-IDF 6.1 构建、`set-target`、烧录、串口监视、6 步自测、首次启动与重启持久化、Settings UI 与五 App／Hardware Test 实机回归——节点 9 的全部运行时验收项均为未验证，不得标记为已完成。已识别的不可构造路径：`nvs_flash_init()`／`nvs_open()` 直接失败导致的降级只能靠源码检查确认（不引入自定义分区表就无法构造），已在 Goal 中记录。
- 2026-09-20 14:08（节点 8 收尾确认，人工执行、Agent 记录）-- 通过：人工确认节点 8 手动测试全部通过，补齐首轮 Monitor 日志未覆盖的 Games 与 PC Monitor 进入／返回验收。未提供补充串口日志，因此本次只记录人工确认，不补造 open／close 时间戳、焦点索引或 `screen children` 数值；节点 8 功能验收无待办，首轮日志的数值证据与其余目视证据仍见 14:03 记录。
- 2026-09-20 14:03（节点 8 实机验证，人工执行、Agent 复核）-- 通过（含一处口径差异，人工确认接受）：人工完成 ESP-IDF 6.1 普通构建、烧录与 Monitor，提供自启动 778 ms 至运行 389095 ms 的完整串口日志，并确认五入口布局、四项菜单与四个状态页、跨页与空槽、两级 B、焦点与页码保持、既有 App 回归“没问题”。日志复核要点：启动链 `Xiaomiao LVGL 9.5 launcher boot` → `Start Xiaomiao launcher` → `launcher created (5 apps)` → `Launcher ready, 5 app(s) registered`，全程无 self-test 标记、无旧 dashboard 启动行；每轮 `<app> opened` 均先于 `launcher: opened '<id>'`，与 Navigation「先发布内容根 → 跑 open 回调 → Manager 发布 current」顺序一致；12 次进入／返回严格配对（Hardware Test 250606／252842、Tools 264189／265443、Settings 267419／291759、307879／309407、310129／313285、321568／324250、381191／382404、383020／383876、384241／384614、384941／385377、385708／386144、386611／389095），无孤立 open、无重复 close；**`screen children=2` 在全部 24 条 open／close 日志上恒定**；**`hardware test closed` 输出 `launcher focus=4 page=1`**（节点 7 时为 3／0），索引 4 与五入口注册顺序一致且 `page=1` 即第 2 页，为该节点最强的数值证据；Settings 10 次会话各只 close 一次，含 24340 ms 长会话与 2682／3156 ms 中长会话及 373／436 ms 短会话，三套 B 语义在同一会话内并存互不干扰；383020→389095 的 6 s 内连续 5 轮 Settings 进入／返回；时间戳 821 ms→389095 ms 单调递增，无 panic／看门狗／重启循环／输入失效。**口径差异**：条款要求“五个 App 各完成至少一次进入／返回”，实测 Games 与 PC Monitor 本轮未进入；经提出后人工明确选择按现有证据接受，理由为两者源码未改动且已在节点 5／6 独立验证，补齐无需重新编译或烧录。**证据类型差异**：索引 4 与页码 2/2 为数值证据；第 1 页四格与页码 1/2、第 2 页空槽、Settings 四项菜单与四个状态页、Settings 索引 3 与返回后停留第 1 页、二级 B 层级变化、Hardware Test 15 页可达（会话 2236 ms，属快速检查）均为目视确认；Settings 按决策 9 不打印 Launcher 焦点索引。本轮未进入 MicroSD 页，未复现 SD／GPIO22 冲突。
- 2026-09-20 13:45（节点 8 实现后静态检查）：`git diff --check` 通过（仅 `ROADMAP.md` 的 LF→CRLF 既有换行提示）；新增文件无行尾空白、无 Tab、纯 ASCII；大括号配平 `xiaomiao_settings.c` 57/57、`xiaomiao_settings.h` 1/1、`main/main.c` 353/353。改动范围核对：新增 `main/apps/settings/` 两个文件，修改 `main/CMakeLists.txt`（`APP_SRCS` 增加 settings 源文件）与 `main/main.c`（新增 include、插入第四个注册调用、补齐 `launcher_boot()` 注释），`git status` 确认未修改 `main/framework/`、`main/apps/games/`、`main/apps/pc_monitor/`、`main/apps/tools/`、`GD32_firmware/`、`sdkconfig.defaults`、`sdkconfig.ci`、`main/idf_component.yml`。App ID 唯一性核对：普通固件 5 个描述 ID 为 `games`、`pc_monitor`、`tools`、`settings`、`hardware_test`，互不重复；注册顺序由 `main/main.c` 的五个 `launcher_register_app()` 调用固定（2447／2452／2457／2462／2467 行）。禁止项检索：`xiaomiao_settings.c` 内无 `nvs_`／`esp_wifi`／`lwip`／`socket`／`xTaskCreate`／`xQueue`／`xSemaphore`／`xEventGroup`／`vTaskDelay`／`esp_timer_`／`gpio_`／`i2c_`／`spi_`／`adc_`／`ledc_`／`sdspi`／`sdmmc`／`fopen`／`lv_screen_load`／`lv_screen_create`；`lv_screen_active()` 仅用于两处只读 `lv_obj_get_child_count()` 生命周期日志，`lv_obj_delete()` 仅在 `open` 自身失败清理时删除自己刚创建的根对象（与节点 7 Tools 一致），不删除 Navigation 内容根。LVGL 9.5 与字形核对（对照本仓库锁定组件）：`LV_SYMBOL_EDIT`＝U+F304 存在于 `lv_symbol_def.h:263-264`，十进制 62212 同时存在于 Launcher 图标字体 `lv_font_montserrat_12.c` 与 `lv_font_montserrat_10.c`。分页规则预核对（对照 `main/framework/xiaomiao_launcher.c:149-184`）：列数 2、每页 4，索引 2 向下 → 4、索引 3 向下 → 4、索引 4 向下保持 4、索引 4 向上 → 2，与节点 8 检查点 4 的预期一致，无需为 Settings 增加特判。按项目分工未执行编译、烧录、串口监视或目标板操作；本条执行时节点 8 的全部运行时验收项均为未验证。
- 2026-09-20 13:28（节点 7 实机验证，人工执行、Agent 复核）-- 通过（含轮次口径差异，人工确认接受）：人工完成 ESP-IDF 6.1 普通构建、烧录与 Monitor，提供自启动 750 ms 至运行 62211 ms 的完整串口日志，并确认四入口布局、Tools 三项菜单与三个详情页、System Info 显示值、Wi-Fi 与 About 页面、两级 B 与长按隔离、既有 App 回归“都测试过了”。日志复核要点：启动链 `Xiaomiao LVGL 9.5 launcher boot` → `Start Xiaomiao launcher` → `launcher created (4 apps)` → `Launcher ready, 4 app(s) registered`，全程无 self-test 标记、无旧 dashboard 启动行；每轮 `<app> opened` 均先于 `launcher: opened '<id>'`，与 Navigation「先发布内容根 → 跑 open 回调 → Manager 发布 current」顺序一致；6 次进入／返回严格配对（Games 46736／47227、PC Monitor 44390／45205、Hardware Test 38985／42599、Tools 6465／12659、14735／36667、48709／62211），无孤立 open、无重复 close；**`screen children=2` 在全部 12 条 open／close 日志上恒定**；`hardware test closed` 输出 `launcher focus=3 page=0`（第四入口索引，节点 6 时为 2）；切换序列 HT(3)→UP→PC Monitor(1)→LEFT→Games(0)→DOWN→Tools(2) 完全符合 2 列 × 2 行规则；Games 491 ms、PC Monitor 815 ms 为短按即返，Hardware Test 3614 ms 超 800 ms 阈值走长按，Tools 3 次会话各只 close 一次且耗时 6194／21932／13502 ms，三套 B 语义在同一会话内并存互不干扰；启动 banner 仅一次、时间戳单调递增，无 panic／看门狗／重启循环／输入失效。**轮次口径差异**：修订后条款要求“Tools 累计 ≥10 次往返”，实测 Tools 3 次、四 App 共 6 次往返（四 App 交替 6 ≥ 5 满足），Tools 往返数未达标；经提出后人工明确选择按现有证据接受，理由为条款实质（open／close 严格成对、`screen children` 稳定、四 App 与三套 B 语义全覆盖、无崩溃与输入失效）已在 6 次往返、12 条日志上完整覆盖；如需补齐无需重新编译或烧录，重新 Monitor 后多按若干轮即可。**证据类型差异**：索引 3 为数值证据；Tools 索引 2 为间接证据（`games closed` 后焦点必为 0，下一次进入即 Tools，0→2 只能由 DOWN 到达）；索引 0／1 为目视与日志顺序一致性证据；System Info／Wi-Fi／About 等视图内容为人工目视确认（视图切换用 `ESP_LOGD`，默认日志级别不输出），Tools 自身不打印 Launcher 焦点索引。本轮未进入 MicroSD 页，未复现 SD／GPIO22 冲突。
- 2026-09-20 13:16（节点 7 实现后静态检查）：`git diff --check` 通过（仅 `ROADMAP.md` 的 LF→CRLF 既有换行提示）；大括号配平 `xiaomiao_tools.c` 71/71、`xiaomiao_tools.h` 1/1、`main/main.c` 352/352。改动范围核对：新增 `main/apps/tools/` 两个文件，修改 `main/CMakeLists.txt`（`APP_SRCS` 增加 tools 源文件、`PRIV_REQUIRES` 增加 `esp_app_format`）与 `main/main.c`（新增 include 与第三个注册调用），未修改 `main/framework/`、`main/apps/games/`、`main/apps/pc_monitor/`、Dashboard 15 页内容与 B 短按／长按状态机、GD32、硬件协议、引脚、ESP-IDF／LVGL 版本或 `dependencies.lock`；未引入第三方依赖、图片、字体或文件系统资源。App ID 唯一性核对：普通固件 4 个描述 ID 为 `games`、`pc_monitor`、`tools`、`hardware_test`，互不重复；注册顺序由 `main/main.c` 的四个 `launcher_register_app()` 调用固定（2445／2450／2455／2460 行）。禁止项检索：`xiaomiao_tools.c` 内无 `esp_wifi`／`socket`／`lwip`／`xTaskCreate`／`xQueue`／`xSemaphore`／`nvs_`／`gpio_`／`i2c_`／`spi_`／`adc_`／`ledc_`／`sdspi`／`fopen`／`lv_screen_load`，仅两处只读 `lv_obj_get_child_count(lv_screen_active())` 用于生命周期日志，不创建或切换 screen。LVGL／IDF 与 API 核对（对照本仓库锁定组件与 IDF 6.1 源码）：`LV_SYMBOL_LIST`＝U+F00B 存在于 `lv_symbol_def.h:47-48` 且 `61451` 在 `lv_font_montserrat_10/12/14.c` 字形表中；`lv_timer_create/delete`、`lv_indev_get_state`、`lv_indev_get_next`、`lv_indev_get_type`、`lv_obj_clean`、`lv_obj_delete`、`LV_LABEL_LONG_MODE_WRAP/DOTS` 均存在；LVGL 9.5 `indev_keypad_proc()`（`lv_indev.c:866-877`、919-922）确认 ESC 只在按下与长按重复时派发按键事件、释放不派发，20 ms timer 轮询 `lv_indev_get_state()` 是唯一可行的释放检测通道，`lv_timer.c:368` 确认 timer 可在自身回调内被删除；`esp_flash_get_size()`（`spi_flash/include/esp_flash.h:79`）、`esp_app_get_description()`（`esp_app_format/include/esp_app_desc.h:55`）、`esp_chip_info()`（`esp_hw_support/include/esp_chip_info.h`）签名与所用字段已逐项核对。发现并处理 1 项阻塞性依赖缺口：`build/compile_commands.json` 中 `main.c` 的编译命令不含 `esp_app_format` 头文件路径，原样写 `#include "esp_app_desc.h"` 必然编译失败，故在 `main/CMakeLists.txt` 的 `PRIV_REQUIRES` 增加 `esp_app_format`（不新增第三方依赖、不改版本）。按项目分工未执行编译、烧录、串口监视或目标板操作；本条目执行时节点 7 的全部运行时验收项均为未验证。
- 2026-09-20 12:52（节点 6 实机验证，人工执行、Agent 复核）-- 通过（含轮次口径差异，人工确认接受）：人工完成 ESP-IDF 6.1 普通构建、烧录与 Monitor，提供 `app_main` 起至运行 99.2 s 的完整串口日志，并确认三入口布局、PC Monitor 骨架页、方向键与 A 无反应、短按 B 返回、Games 与 Hardware Test 回归“都没问题”。日志复核要点：启动链 `Xiaomiao LVGL 9.5 launcher boot` → `Start Xiaomiao launcher` → `launcher created (3 apps)` → `Launcher ready, 3 app(s) registered`，全程无 self-test 标记、无旧 dashboard 启动行；三类 App 每轮 `<app> opened` 均先于 `launcher: opened '<id>'`，与 Navigation「先发布内容根 → 跑 open 回调 → Manager 发布 current」顺序一致；8 轮进入／返回严格配对（Games 2、PC Monitor 4、Hardware Test 2），无孤立 open、无重复 close；**`screen children=2` 在全部 16 条 open／close 日志上恒定**，证明每次关闭后内容根完整释放；启动 banner 仅一次、时间戳 1554 ms→99201 ms 单调递增，无 panic／看门狗／重启循环／输入失效；Hardware Test 2 次 `close` 全部输出 `launcher focus=2 page=0`（与第三入口索引一致，且为该索引的数值证据）；Games 1256／1851 ms 与 PC Monitor 1648／1903／8482／10352 ms 均无长按语义、B 由 Launcher 转发分支按下即返回，Hardware Test 3124／7745 ms 均超 800 ms 阈值，三套 B 路径在同一会话内并存互不干扰。**证据类型差异**：只有 Hardware Test 的 close 路径打印 `launcher focus=`，焦点索引 0（Games）与 1（PC Monitor）为人工目视加日志顺序一致性证据，非数值证据，与节点 5 对 Games 的口径一致。**轮次口径差异**：条款要求“连续 10 轮 PC Monitor 进入／返回及至少 5 轮三 App 交替”，实测 PC Monitor 4 轮（最长不中断连续段 2 轮）、Games 2 轮、Hardware Test 2 轮共 8 轮，两个数量口径未满足；经提出后人工明确选择按现有证据接受，理由为条款实质目的（open／close 成对、`screen children` 稳定、无崩溃与输入失效）已在 8 次应用往返、16 条日志中完整覆盖且三 App 与两种 B 语义均已覆盖。本轮未进入 MicroSD 页，未复现 SD／GPIO22 冲突。
- 2026-09-20 12:42（节点 6 实现后静态检查）：`git diff --check` 通过（仅 `ROADMAP.md` 的 LF→CRLF 既有换行提示）；大括号配平 `xiaomiao_pc_monitor.c` 18/18、`xiaomiao_pc_monitor.h` 1/1、`main/main.c` 351/351。改动范围核对：新增 `main/apps/pc_monitor/` 两个文件，修改 `main/CMakeLists.txt` 与 `main/main.c`，未修改 `main/framework/` 下节点 1～3 文件、`main/apps/games/`、Dashboard 15 页内容与 B 短按／长按状态机、GD32、硬件协议、引脚、依赖版本或 `dependencies.lock`；未新增组件或第三方依赖。禁止项检索：`xiaomiao_pc_monitor.c` 内无 `lv_group`／`lv_obj_add_event_cb`／`xTaskCreate`／`esp_timer`／`vTaskDelay`／`xQueue`／`xSemaphore`／`xEventGroup`／`gpio_`／`i2c_`／`spi_`／`adc_`／`ledc_`／`nvs_`／`sdspi`／`fopen`／`lv_obj_delete`／`esp_random`／`esp_wifi`／`socket`／`lwip`，仅两处只读 `lv_obj_get_child_count(lv_screen_active())`，不创建或切换 screen。App ID 唯一性核对：普通固件 3 个描述 ID 为 `games`、`pc_monitor`、`hardware_test`，互不重复。LVGL 9.5 API 与字形核对：`lv_obj_set_pos`／`lv_obj_set_size`／`lv_text_align_t`／`LV_TEXT_ALIGN_LEFT|CENTER|RIGHT` 均存在于本仓库锁定组件，`LV_SYMBOL_EYE_OPEN`（61550）与 `°`（U+00B0）均在 `lv_font_montserrat_12.c` 的 `-r` 子集内。按项目分工未执行编译、烧录、串口监视或目标板操作；本条执行时节点 6 运行时验收项全部未验证，后续结果见 12:52 条目。
- 2026-09-20 12:29（节点 5 实机验证，人工执行、Agent 复核）-- 通过（含两处数量口径差异，人工确认接受）：人工完成 ESP-IDF 6.1 普通构建、烧录与 Monitor，提供自启动至运行 173 s 的完整串口日志，并确认双入口布局、Games 三段文本、Games 内方向键与 A 无反应、短按 B 返回、Hardware Test 短按／长按 B 与 15 页回归“都没问题”。日志复核要点：启动链输出 `launcher: launcher created (2 apps)` 与 `Launcher ready, 2 app(s) registered`，全程无 self-test 标记、无旧 dashboard 启动行；每轮 `games opened`／`hardware test opened` 均先于 `launcher: opened '<id>'`，与 Navigation「先发布内容根 → 跑 open 回调 → Manager 发布 current」的实现顺序一致；Games 10 轮与 Hardware Test 4 轮 open/close 严格配对（无孤立 open、无重复 close）；**`screen children=2` 在全部 28 条 open／close 日志上恒定**，证明每次关闭后内容根完整释放；启动 banner 仅一次、时间戳 803 ms→173147 ms 单调递增，无 panic／看门狗／重启循环／输入失效；Games 每轮耗时 3.35/1.90/0.78/0.42/0.49/0.70/0.58/0.61/0.61/0.53 s（后 8 轮 <1 s，证明短按 B 即可返回，走 Launcher 的 App 打开态 B 转发分支），Hardware Test 每轮 3.60/3.47/2.95/1.91 s（全部超过 800 ms 阈值），两套 B 语义在同一会话内并存且互不干扰；Hardware Test 4 次 `close` 全部输出 `launcher focus=1 page=0`，**首次为节点 4 无法证明的焦点保持取得数值证据**。**数量口径差异**：验收条款要求“连续 10 轮 Games 进入／返回及至少 5 轮双 App 交替”，实测为 10 轮 Games（最长不中断连续段 4 轮）与 4 轮 Hardware Test，两个数量口径未满足；经提出后人工明确选择按现有证据接受，理由为条款实质目的（open／close 成对、`screen children` 稳定、无崩溃与输入失效）已在 14 次应用往返中完整覆盖，差异属计数口径而非功能或对象生命周期缺陷。本轮未进入 MicroSD 页，未复现 SD／GPIO22 冲突。
- 2026-09-20 12:15（节点 5 实现后静态检查）：`git diff --check` 通过（仅 ROADMAP 的 CRLF 提示），大括号配平（`xiaomiao_games.c` 9/9、`xiaomiao_games.h` 1/1、`main/main.c` 350/350）。逐项核对改动只落在新增 `main/apps/games/` 两个文件与 `main/CMakeLists.txt`、`main/main.c`、文档：未修改 `main/framework/` 下节点 1～3 文件、Dashboard 15 页内容与 B 状态机、GD32、硬件协议、引脚、依赖版本或 `dependencies.lock`。`xiaomiao_games.c` 内无 `lv_group`／`lv_obj_add_event_cb`／`xTaskCreate`／`esp_timer`／`vTaskDelay`／队列锁／`gpio_`／`i2c_`／`spi_`／`adc_`／`ledc_`／`nvs_`／`sdspi`／`fopen`／`lv_obj_delete`，仅两处只读 `lv_obj_get_child_count(lv_screen_active())` 用于决策 12 的生命周期日志；未创建或切换 screen，未加入 group。核对本仓库锁定的 LVGL 9.5 头文件确认所用 API 与 `lv_font_montserrat_10/12/14`、`LV_SYMBOL_PLAY` 均存在（字体由 `sdkconfig.defaults` 的 `CONFIG_LV_FONT_MONTSERRAT_10/12/14=y` 启用）。发现并修正 1 项阻塞性缺口：`main/CMakeLists.txt` 原为 `INCLUDE_DIRS ""`，`main/` 自身不在 include 路径上（已从 `build/compile_commands.json` 核实无 `-I<main>`），游戏模块无法按 Goal 决策 2 写 `#include "framework/xiaomiao_app.h"`，故改为 `INCLUDE_DIRS "."`。按项目分工未执行编译、烧录、串口监视或目标板操作；本条执行时节点 5 运行时验收项全部为未验证，后续结果见 12:29 条目。
- 2026-09-20 11:59（节点 5 施工文档复核）：对照路线图、项目概览、v0.1 设计文档、节点 1～4 Goal、App／Navigation 接口、Launcher App 打开态 B 转发分支和当前普通启动注册链，确认节点 5 只交付可进入／返回的 Games 占位页，不实现真实游戏、不创建独立 Task、不接触硬件；固件源码、编译、烧录、串口和实机行为均未验证。
- 2026-09-20 11:46（节点 4 收口判定，人工确认、Agent 复核）：人工确认蜂鸣器实际发声正常、本机无电机硬件。据此判定“从电机或蜂鸣器运行状态退出时先停止持续输出”在当前硬件上不可构造：电机侧 `s_board.motor_running[]` 仅在 `gd32_motor_set()` 返回 `ESP_OK` 时置真（`ui_motor_toggle()`），无电机且 GD32 未实现 I2C `0x40` 从机协议，该标志从未为真，`hardware_test_close()` 的 `if (!s_board.motor_running[motor]) continue;` 跳过停止命令；蜂鸣器侧 A 键调用 `buzzer_beep(s_buzzer_freq_hz, 140)`（`main/main.c:2065`），140 ms 后由 `s_buzzer_stop_at` 在 `hardware_process_timers()` 中自动停止，不存在“仍在发声”时按 B 的时刻，且 `keypad_read_cb()` 为单键模型无法同时按住 A 与 B。`buzzer_stop()` 在每次 `close` 均被调用、代码路径已执行但无可观察效果。按本 Goal 失败路径条款（设备缺失只标记对应外设未验证、不阻塞）标记该条款未验证并收口节点 4；同时在 Goal 与路线图中明确 LED、电机、MPU6050 的实机行为仍无证据，不得视为外设回归已通过。
- 2026-09-20 11:43（节点 4 最终生命周期轮转与人工确认，人工执行、Agent 复核）：同一 Monitor 会话内 16 轮 `hardware test opened`／`hardware test closed` 全部配对（16/16），其中**前 11 轮为一次不中断的快速轮转**（每轮 1.09～1.29 s），满足“连续至少 10 轮”。**`screen children=2` 在全部 16 次 open 与 16 次 close 上恒定**，无一次增长，证明 Navigation 每次都在关闭后完整释放 App 内容根、Dashboard 子对象无泄漏，也即“连续 10 轮后 screen 子对象保持基线”成立；16 轮无崩溃、无 panic、无重启循环、无输入失效。`launcher focus=0 page=0` 全程不变，但**该项本轮不具证明力**（普通固件只注册 1 个 App，焦点无处可移、只有 1 页，恒为 0/0），Launcher 状态保留只能靠行为观察。人工确认：返回 Launcher 后按各方向键只有 Launcher 侧反应、无任何 Dashboard 反应（证明焦点已交回、周期循环未触碰失效 UI）；B 短按逐页给出原有提示且均未退出；按住约 0.3 s 出现一次 `Hold to exit`、达 800 ms 返回、松开无补发、提示不重复；15 页全部可进入、翻页与操作。MicroSD 页 2 次挂载失败后 2.7 s 仍正常返回，既存 SD／GPIO22 问题再次复现。当时仍缺“电机／蜂鸣器运行状态下退出先停输出”，已在 11:46 定性。
- 2026-09-20 11:32（节点 4 生命周期轮转与 MicroSD 缺失路径，人工执行、Agent 复核）：同一 Monitor 会话内 19 轮 `hardware test opened`／`hardware test closed` 严格配对，无孤立 open、无重复 close、无 panic、无重启循环；快速轮转段最长连续 6 轮（每轮 1.2～1.5 s），另有 4 轮与 3 轮连续段，长时段停留（35.8 s、25 s、15.4 s、16.2 s）后仍正常返回。`main/main.c` 中 `xiaomiao_navigation_back()` 只有第 1153 行一处调用（B 手势退出分支），`xiaomiao_launcher.c:253` 的 Launcher 转发分支在 Dashboard 持有焦点时不可达，因此 19 次返回全部由 B 长按手势触发且每次只产生一次关闭。第 19 轮内 MicroSD 页按 A 三次，3 次 `sdmmc_card_init failed (0x107)` 与 `gpio: conflict found for GPIO[22]`，1.7 s 后仍正常返回 Launcher——既未阻塞启动也未阻塞返回，符合设计文档第 9 节“SD 失败则 SD_UNAVAILABLE、记录日志、继续启动 Launcher”的既定策略；`sd_try_mount()` 只由 A 键的 `ui_action()` 调用（`main/main.c:2077`），不存在自动重试循环。该 SD／GPIO22 组合是设计文档已记录的既存问题（`PIN_NUM_SD_CS = GPIO_NUM_22`），本次为首次实机复现，不属节点 4 范围。当时仍缺 screen 子对象基线与 Launcher 焦点／页码（尚未补日志）、连续 10 轮、B 短按与提示目视确认、15 页与缺失设备回归。
- 2026-09-20 11:20（节点 4 检查点 1 实机验证，人工执行、Agent 复核）：人工编译并烧录后 Monitor 输出完整。应用信息 `App version: e003e2e-dirty`、`Compile time: Sep 20 2026 11:20:14`、`ESP-IDF: v6.1`，说明烧录的固件确含本次未提交改动。启动日志出现 `Xiaomiao LVGL 9.5 launcher boot` 与 `Start Xiaomiao launcher`，随后 `launcher: launcher created (1 apps)` 与 `Launcher ready, 1 app(s) registered`；全程未出现 `Self test build`／`Navigation self test build`／`Launcher self test build` 三个自测标记，也未出现旧的 `Start Xiaomiao hardware dashboard`，且没有 `hardware test opened`——说明普通构建走的是 Launcher 路径、Registry 只有 1 个正式 App、开机未创建 Dashboard。LVGL 显示初始化输出 `160x128, dpi=60, 3 full-screen DMA buffers, SPI=60 MHz`，无非预期错误、无 panic、无重启循环。检查点 1 全部满足。
- 2026-09-20 11:18（节点 4 实现后静态检查）：`git diff --check` 通过（仅 ROADMAP 的 CRLF 提示），大括号配平（346/346）。逐项核对改动只落在 `main/main.c` 与文档：未修改 `main/framework/` 下节点 1～3 文件、GD32、硬件协议、引脚、依赖版本或 `dependencies.lock`；未新增源文件，因此未改 `main/CMakeLists.txt`。确认 `ui_create()` 只剩一处调用（`hardware_test_open`）且传入 Navigation 内容根；`ui_key_event_cb()` 中 `LV_KEY_ESC` 立即分支已移除，B 只由 `hardware_test_b_gesture_update()/poll()` 决策；`lv_indev_set_long_press_time()`／`repeat_time` 未改；`hardware_update()`／`ui_refresh()` 已由 `hardware_test_is_open()` 门控，`hardware_process_timers()` 保持常驻；`hardware_update()` 只做 ADC／I2C 探测／MPU 采样，不含电机或蜂鸣器安全逻辑，门控不引入安全缺口。按项目分工未执行编译、烧录、串口监视或目标板操作；本条目执行时节点 4 运行时验收项均为未验证，后续进展见 11:20 条目。
